package com.github.davidch93.etl.batch.cli

/**
 * The `Command` object defines the command names used in the ETL Batch CLI application.
 * These commands determine the type of pipeline to execute, such as creating data pool.
 *
 * @author david.christianto
 */
object Command {
  final val DATA_POOL_CMD = "create_data_pool"
}
